import 'package:flutter/material.dart';

abstract class AppThemeProperties {
  //Colors
  Color get primaryColorDark;
  Color get accentColor;
  Color get backgroundColor;
  Color get footerColor;
  Color get textColor;
  Color get warningColor;
  Color get backgroundTooltipColor;
  Color get secondaryBackgroundCardColor;
  Color get successBackgroundColor;
  Color get backgroundField;
  Color get backgroundIcon;
  Color get breadcrumbBackgroundColor;
}
